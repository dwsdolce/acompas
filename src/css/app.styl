// app global css

body
  font-size 16px
  line-height 1.3em

body.cordova
  .q-layout
    /* iOS safe area */
    padding-bottom: constant(safe-area-inset-bottm)
    padding-bottom: env(safe-area-inset-bottom)

.caption
  letter-spacing .2px
  margin-bottom 6px

.q-field__control
  height 42px

.q-btn
  line-height 1.3em
